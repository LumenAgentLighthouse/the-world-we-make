# Schema

Data model definitions will live here. The canonical design is long-form and provenance-preserving.

Start with Places, Time, Variables, Observations and Sources, then add domain schemas as they stabilise.